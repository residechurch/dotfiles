name: Actionlint

on:
  push:
    branches:
      - main
  pull_request:
  merge_group:

defaults:
  run:
    shell: bash -xeuo pipefail {0}

concurrency:
  group: "actionlint-${{ github.ref }}"
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions: {}

jobs:
  workflow_syntax:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      osv-sarif: ${{ steps.osv.outputs.sarif }}
    steps:
      - name: Set up Homebrew
        uses: Homebrew/actions/setup-homebrew@3cdb78d0f62ad29dd32de765782654f4eedea607 # 2026.08.31.1
        with:
          core: false
          cask: false

      - uses: Homebrew/actions/cache-homebrew-prefix@3cdb78d0f62ad29dd32de765782654f4eedea607 # 2026.08.31.1
        with:
          install: actionlint shellcheck zizmor osv-scanner
          uninstall: true

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - run: zizmor .

      - run: actionlint

      - name: Run OSV Scanner
        id: osv
        run: |
          rc=0
          osv-scanner --format sarif --output osv-results.sarif --recursive . || rc=$?
          if [[ "${rc}" -eq 128 ]]; then
            echo "sarif=false" >> "${GITHUB_OUTPUT}"
          elif [[ "${rc}" -ne 0 ]]; then
            echo "sarif=true" >> "${GITHUB_OUTPUT}"
            exit "${rc}"
          else
            echo "sarif=true" >> "${GITHUB_OUTPUT}"
          fi
        continue-on-error: true

      - name: Upload OSV Scanner SARIF artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always() && steps.osv.outputs.sarif == 'true' && github.event_name != 'merge_group'
        with:
          name: osv-results.sarif
          path: osv-results.sarif
          if-no-files-found: ignore

      - name: Add OSV summary
        if: always() && steps.osv.outputs.sarif == 'true'
        run: |
          {
            echo "## OSV Scanner"
            if [[ -f osv-results.sarif ]]; then
              count="$(jq '.runs[0].results | length' osv-results.sarif 2>/dev/null || echo 0)"
              echo "- Findings: ${count}"
              if [[ "${count}" -gt 0 ]]; then
                echo ""
                echo "<details><summary>Top findings (up to 20)</summary>"
                jq -r '.runs[0].results[:20][] | "- \(.ruleId // "unknown"): \(.message.text // "No message")"' osv-results.sarif 2>/dev/null || true
                echo "</details>"
              fi
            else
              echo "- No SARIF output generated."
            fi
          } >> "${GITHUB_STEP_SUMMARY}"

      - run: zizmor --format sarif . > results.sarif
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload SARIF file
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always() && github.event_name != 'merge_group'
        with:
          name: results.sarif
          path: results.sarif

  upload_sarif:
    needs: workflow_syntax
    if: >
      always() &&
      !contains(fromJSON('["cancelled", "skipped"]'), needs.workflow_syntax.result) &&
      !github.event.repository.private &&
      github.event_name != 'merge_group'
    runs-on: ubuntu-slim
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Download SARIF file
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: results.sarif
          path: results.sarif

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
        with:
          sarif_file: results.sarif
          category: zizmor

  osv_upload_sarif:
    needs: workflow_syntax
    if: >
      always() &&
      needs.workflow_syntax.outputs.osv-sarif == 'true' &&
      !contains(fromJSON('["cancelled", "skipped"]'), needs.workflow_syntax.result) &&
      !github.event.repository.private &&
      github.event_name != 'merge_group'
    runs-on: ubuntu-slim
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Download OSV Scanner SARIF artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: osv-results.sarif
          path: osv-results.sarif

      - name: Upload OSV Scanner SARIF to Code Scanning
        uses: github/codeql-action/upload-sarif@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
        with:
          sarif_file: osv-results.sarif
          category: osv-scanner

  dependency_review:
    # Flags newly-introduced vulnerable dependencies on PRs before they land on main
    # Requires GitHub Advanced Security on private repos — skip silently if not available
    if: github.event_name == 'pull_request' && !github.event.repository.private
    runs-on: ubuntu-slim
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Dependency Review
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
